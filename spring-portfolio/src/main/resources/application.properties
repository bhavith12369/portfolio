server.port=${PORT:8082}

# Comma-separated origins for split hosting (e.g. GitHub Pages frontend + this API).
# Example: https://bhavith12369.github.io,https://username.github.io
portfolio.cors.allowed-origins=${PORTFOLIO_CORS:}
